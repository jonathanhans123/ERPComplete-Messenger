/// Default production API — override via [AuthRepository.saveApiBaseUrl].
class ApiConfig {
  static const defaultBaseUrl = 'https://srv1804550.hstgr.cloud/api/v1';
  static const apiBaseUrlKey = 'api_base_url';
}
